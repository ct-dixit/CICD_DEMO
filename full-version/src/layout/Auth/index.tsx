import { ReactNode } from 'react';

interface Props {
  children: ReactNode;
}

// ==============================|| AUTH LAYOUT ||============================== //

export default function AuthLayout({ children }: Props) {
  return children;
}
